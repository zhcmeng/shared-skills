## 改文件：读用 Read，改用 Edit / Write

**任何权限模式下都适用，包括 bypass permissions（跳过权限确认）。**

判断标准是这次改动有没有留下逐处的前后对比：Edit / Write 每次调用都会留下，
用 Bash 改完只剩一个结果，中间改了什么看不出来。

那是我复核你改了什么、也是改错了回退的依据。

例外：同一个替换要横跨很多文件时（批量改名、全仓替换），用脚本更合适。
动手前说明要改哪些文件、改什么。
