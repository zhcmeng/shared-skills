---
type: llm
focus: { source: file, path: notes.md }
---
通过的条件：改后的文件里，"命中""落盘""回源""链路""overhead"这类要读者解码的说法都没了，换成了不用解码的说法（例如"缓存里有就直接跳过""写进磁盘""去源头取一份""整个流程多出来的开销"）。

正文、图注、mermaid 图里的文字都算：这三处里留下任何一个原词，就不通过。frontmatter 里和引文里出现的那几个词不算——那两处不审。
