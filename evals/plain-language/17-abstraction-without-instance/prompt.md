---
max_turns: 12
timeout_seconds: 180
allowed_tools: [Skill, Read, Glob]
model: opus
runs: 3
---
下面是你刚才给我的回答，我原样贴过来：

【
**从「生成前没问时间范围」这样一句话，到一条机器能执行的判断。**

第 02 课结束时，你手上是一张失败模式表：名字加条数，按修改成本排了序。
但表上那句「生成前没问时间范围」，机器执行不了——它是写给人看的，不是写给程序看的。

这一课做的事就一件：**把名字改成一句能判真假的话**。
改完，它才能反复跑、才能在你每次改动之后告诉你有没有变差。
】

把名字改成一句能判真假的话，我有点难理解。看不懂，重说一遍。
