---
type: llm
focus: last_message
---
看的是重说出来的那段正文——读者会从头读到尾的那段连续文字。回答里如果还另附了改动清单或拿不准清单，这一条不判它们：只拿正文判，那些附加内容里出现的词句不算正文里的。

通过的条件：正文不再让读者去"方括号里"找东西——说清了要对着哪一段文本找（表里代码样式的那些字样），并且不让读者把 `阶段`、`消息` 当成能照着搜的字面文本（说明它们是占位、要换成实际内容）。

"方括号里的字面文本可以直接拿去搜"原样留着，或者改完仍然没说清 `阶段`、`消息` 这类是占位，就不通过。

（维护提示，判分时忽略：本文件与其它判据文件有逐字相同的段落，改一处要全改——重复清单见 ../../README.md。）
