# 测试用例规格说明

测试项：`plugin/skills/token-counter/scripts/count_tokens.py`。四门选定的技术共导出 45 个测试覆盖项（等价类划分 20 个、边界值分析 5 个、命令行与输入来源判定表 9 个、引擎就绪判定表 5 个、端到端场景 6 个），导出 29 条测试用例。这 29 条已经落成可跑的代码，在 `tests/token-counter/test_count_tokens.py`，跑法 `bash tests/run.sh token-counter`；方法名里的 TC 编号就是这里的编号，样本按测试数据需求的编号建。

代码有两处与下面这张表不是逐字对应，理由都写在那个文件的头上，读这份文档的人先知道一下，免得照着表去执行时对不上：

——TC-22～TC-25 这四条，表里按干净环境写（新建一个没装引擎的环境、真的装一次、真的断网）。代码测的是同一张判定表的判定与动作：把测试项导进来，替掉「本机能不能导入引擎」「本地有没有 wheel」「装得上装不上」这三处外部条件，验它走哪条路、发出去的是什么命令。干净环境那一层仍按 TP-4 的启动栏另走一次。

——TC-15 那条，表里固定的要求是两条通道给出同一个数（都应为 `tokens: 2`、`chars: 4`）。代码钉的是实测的这两组值：文件通道 2／4、字面文本通道 3／5——测试套件不该常红，那处要求成不成立留待定口径。

风险等级照开工输入的风险信息判：正文落在依据点名的四类形态（含空格、含中文、以汉字开头、含官方特殊 token）里的、无效输入与依据未规定的输入、计数口径与三条通道之间的一致性，以及边界值分析识别出的全部边界值，判高；其余判中。一条用例覆盖多个覆盖项时，用例的等级取其中最靠上的那一级。

各条用例的判定看三样：标准输出上的内容、标准错误上的内容、进程的退出码（ENV-9）。

按不可行项剔除的是两条，都不进下面的覆盖项清单：一条是依据未规定的用法——没给字面文本、也没给文件路径，标准输入又接着一台终端（这时测试项会一直等标准输入，用例没有可判定的预期结果，也没法在无人值守下自己结束）；另一条是正文字符长度边界下侧的「−1 个字符」——一段文本不可能有负数个字符，这一侧没有对应的输入。两条的剔除原因就写在这里。

## 一、测试覆盖项

| 唯一标识符 | 英文名 | 描述 | 风险等级 | 可追溯性 |
|:---|:---|:---|:---|:---|
| TCOV-1 | ascii_words_with_spaces | 正文是纯 ASCII 文本、词与词之间有空格（E1） | 高 | TM-1 的 E1 |
| TCOV-2 | cjk_text | 正文含中文，汉字与中文标点（E2） | 高 | TM-1 的 E2 |
| TCOV-3 | leading_cjk_then_ascii | 正文以汉字开头、后面接 ASCII（E3） | 高 | TM-1 的 E3 |
| TCOV-4 | official_special_token | 正文里含官方词表的特殊 token（E4） | 高 | TM-1 的 E4 |
| TCOV-5 | empty_text | 空正文，0 个字符（E5） | 中 | TM-1 的 E5 |
| TCOV-6 | whitespace_only | 正文只有空白字符（E6） | 中 | TM-1 的 E6 |
| TCOV-7 | code_text | 正文是代码文本（E7） | 中 | TM-1 的 E7 |
| TCOV-8 | astral_plane_char | 正文含补充平面上的字符（E8） | 中 | TM-1 的 E8 |
| TCOV-9 | bom_not_counted | 文件开头的编码标记 BOM 不计入正文（E9） | 中 | TM-1 的 E9 |
| TCOV-10 | leading_trailing_spaces | 正文首尾带空格（E10） | 中 | TM-1 的 E10 |
| TCOV-11 | file_not_utf8 | 文件里的字节不是合法 UTF-8（N1） | 高 | TM-1 的 N1 |
| TCOV-12 | missing_path | 文件路径不存在（N2） | 高 | TM-1 的 N2 |
| TCOV-13 | path_is_directory | 路径指向一个目录（N3） | 高 | TM-1 的 N3 |
| TCOV-14 | stdin_not_utf8 | 标准输入里的字节不是合法 UTF-8（N4） | 高 | TM-1 的 N4 |
| TCOV-15 | literal_text_leading_bom | 字面文本以 U+FEFF 开头，依据没规定这种情况该怎么算（N6） | 高 | TM-1 的 N6 |
| TCOV-16 | tokens_match_official_vocab | `tokens` 的取值与随包词表的规范语义一致（O1） | 高 | TM-1 的 O1 |
| TCOV-17 | chars_counts_characters | `chars` 计的是字符个数，不是 UTF-8 字节数（O2） | 高 | TM-1 的 O2 |
| TCOV-18 | two_lines_on_valid_input | 有效输入产出 `tokens` 与 `chars` 两行（O3） | 中 | TM-1 的 O3 |
| TCOV-19 | no_count_on_invalid_input | 无效输入不产出计数行，以非 0 状态结束（O4） | 高 | TM-1 的 O4 |
| TCOV-20 | three_channels_agree | 同一段正文经三条输入通道算出的两个数相同（O5） | 高 | TM-1 的 O5 |
| TCOV-21 | text_length_zero | 边界值：正文长度 0 个字符（边界上的值） | 高 | TM-2 的字符长度边界 |
| TCOV-22 | text_length_one | 边界值：正文长度 1 个字符（边界外一个增量） | 高 | TM-2 的字符长度边界 |
| TCOV-23 | two_leading_bytes | 边界值：文件开头 2 个字节，少一个字节的编码标记（边界下侧） | 高 | TM-2 的字节边界 |
| TCOV-24 | three_leading_bytes | 边界值：文件开头 3 个字节，正好一个完整的编码标记（边界上的值） | 高 | TM-2 的字节边界 |
| TCOV-25 | four_leading_bytes | 边界值：文件开头 4 个字节，编码标记后接一个字节（边界上侧） | 高 | TM-2 的字节边界 |
| TCOV-26 | rule_text_given | 判定规则 1：命令行给了 `--text` 时，按这段字面文本计数并输出两行 | 中 | TM-3 的规则 1 |
| TCOV-27 | rule_file_read | 判定规则 2：给了文件路径、文件读得出来、字节能解码时，按文件计数并输出两行 | 中 | TM-3 的规则 2 |
| TCOV-28 | rule_file_not_utf8 | 判定规则 3：文件读得出来但字节不是合法 UTF-8 时，不给计数结果，以非 0 状态结束 | 高 | TM-3 的规则 3 |
| TCOV-29 | rule_file_unreadable | 判定规则 4：文件读不出来（路径不存在，或路径是目录）时，不给计数结果，以非 0 状态结束 | 高 | TM-3 的规则 4 |
| TCOV-30 | rule_stdin | 判定规则 5：既没给 `--text` 也没给路径、标准输入接得上时，按标准输入计数；输入为空则给出 0 | 中 | TM-3 的规则 5 |
| TCOV-31 | rule_stdin_not_utf8 | 判定规则 6：标准输入里的字节不是合法 UTF-8 时，不给计数结果，以非 0 状态结束 | 高 | TM-3 的规则 6 |
| TCOV-32 | engine_already_importable | 引擎判定规则 1：本机已经可以导入引擎时直接使用，不装、不联网，标准错误上不出安装提示 | 中 | TM-4 的规则 1 |
| TCOV-33 | engine_install_bundled_wheel | 引擎判定规则 2：本机没装、随包 wheel 在且装得上时，离线安装这个 wheel | 中 | TM-4 的规则 2 |
| TCOV-34 | engine_wheel_fails_then_pypi | 引擎判定规则 3：本机没装、随包 wheel 装不上、联网装得上时，转去联网安装指定版本 | 中 | TM-4 的规则 3 |
| TCOV-35 | engine_pypi_direct | 引擎判定规则 4：本机没装、`wheels/` 下没有 wheel、联网装得上时，直接联网安装指定版本 | 中 | TM-4 的规则 4 |
| TCOV-36 | engine_both_paths_fail | 引擎判定规则 5：两条安装路都不通时，以非 0 状态结束，不产出计数两行 | 中 | TM-4 的规则 5 |
| TCOV-37 | scenario_main_file | 场景：已经装好引擎的机器上数一个文件（主场景） | 中 | TM-5 的主场景 |
| TCOV-38 | scenario_text_on_command_line | 场景：命令行直接给一段文本（备选场景） | 中 | TM-5 的备选场景 1 |
| TCOV-39 | scenario_piped_output | 场景：把别的命令的输出接到管道（备选场景） | 中 | TM-5 的备选场景 2 |
| TCOV-40 | scenario_offline_first_run | 场景：没有引擎、不能联网的机器上首次使用（备选场景） | 中 | TM-5 的备选场景 3 |
| TCOV-41 | scenario_wheel_not_applicable | 场景：没有引擎、随包 wheel 不适用的机器上首次使用（备选场景） | 中 | TM-5 的备选场景 4 |
| TCOV-42 | scenario_invalid_utf8_file | 场景：给的文件不是合法 UTF-8（备选场景） | 高 | TM-5 的备选场景 5 |
| TCOV-43 | multiple_sources_text_and_file | 输入来源同时给出一路以上：`--text` 与文件路径同时给出，或 `--text` 与标准输入同时可用，依据没规定按哪一路算 | 高 | TM-3 的规则 1（多路同时成立时的形态） |
| TCOV-44 | multiple_sources_file_and_stdin | 输入来源同时给出一路以上：文件路径与标准输入同时可用，依据没规定按哪一路算 | 高 | TM-3 的规则 2（多路同时成立时的形态） |
| TCOV-45 | empty_path_string | 文件路径给空字符串，依据没规定这种情况该怎么算 | 高 | TM-3 的条件 C2 与规则 5 |

## 二、测试用例

### （一）正文内容与计数口径（TM-1 的等价类）

本组各条共用的前置条件：ENV-1 那台机器上 ENV-2 的词表与 ENV-3 的 wheel 都在位，引擎已经装好（`tokenizers` 可以导入）；ENV-4 的工作区里按各条要用的样本备好文件。样本见测试数据需求。

| 唯一标识符 | 英文名 | 目标 | 风险等级 | 前置条件 | 输入 | 预期结果 |
|:---|:---|:---|:---|:---|:---|:---|
| TC-1 | cjk_literal_pins_chars_semantics | 命令行直接给一段中文文本时按字面文本计数，并钉住 `chars` 的口径 | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "你好，世界"`（按 DATA-1 给） | 标准输出恰好两行：`tokens: 3`、`chars: 5`；标准错误为空；退出码 0。这段正文在 UTF-8 里占 15 字节，`chars` 给的是 5，不是 15 |
| TC-2 | ascii_words_with_spaces | 正文是纯 ASCII、词与词之间有空格时，`tokens` 与随包词表一致 | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "The quick brown fox jumps over the lazy dog"`（按 DATA-2 给） | 标准输出两行：`tokens: 9`、`chars: 43`；标准错误为空；退出码 0 |
| TC-3 | leading_cjk_then_ascii | 以汉字开头、后面接 ASCII 的正文按词表计数，不被拆散也不落空 | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "中文和 English 混排"`（按 DATA-3 给） | 标准输出两行：`tokens: 6`、`chars: 14`；退出码 0。若这段正文数出 0 个 token，判为缺陷 |
| TC-4 | official_special_token | 正文里的官方特殊 token 按词表整体计入，不被拆成多个 token | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "<｜end▁of▁sentence｜>"`（按 DATA-4 给） | 标准输出两行：`tokens: 1`、`chars: 19`；退出码 0。这串字符若被拆成多个 token，判为缺陷 |
| TC-5 | empty_stdin | 标准输入接上却读不到字节（管道为空）时按空正文给出 0，不报错 | 中 | 见本组共用前置条件；命令行外壳能接管道与重定向（ENV-8） | 把 0 个字节接到标准输入后执行 `python count_tokens.py`（`printf ''` 的输出接进来） | 标准输出两行：`tokens: 0`、`chars: 0`；标准错误为空；退出码 0 |
| TC-6 | whitespace_only | 正文只有空白字符时的计数 | 中 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "$(printf '   \n\t\n  ')"`（按 DATA-5 取那段正文；里面的换行与制表符要真的传进去，写法见 DATA-5） | 标准输出两行：`tokens: 3`、`chars: 8`；退出码 0 |
| TC-7 | code_text | 正文是一段代码时的计数 | 中 | 见本组共用前置条件 | 把 DATA-6 那段代码文本写成一个文件，执行 `python count_tokens.py 代码样本.py` | 标准输出两行：`tokens: 12`、`chars: 32`；退出码 0 |
| TC-8 | astral_plane_char | 正文含补充平面上的字符时，`chars` 仍按字符个数算 | 中 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "emoji 😀 与中文混排"`（按 DATA-7 给） | 标准输出两行：`tokens: 9`、`chars: 13`；退出码 0。这段正文在 UTF-8 里占 26 字节，`chars` 给的是 13，不是 26 |
| TC-9 | file_bom_not_counted | 文件开头的编码标记不计入正文 | 中 | 见本组共用前置条件 | 执行 `python count_tokens.py 带标记.md`（DATA-10 那个文件） | 标准输出两行：`tokens: 2`、`chars: 4`；退出码 0。去掉开头 3 个字节之后的正文是「中文测试」4 个字符；若把这 3 个字节也算进正文（`chars: 7`），判为缺陷 |
| TC-10 | leading_trailing_spaces | 正文首尾带空格时的计数 | 中 | 见本组共用前置条件 | 执行 `python count_tokens.py --text " hello world "`（按 DATA-8 给） | 标准输出两行：`tokens: 3`、`chars: 13`；退出码 0 |

### （二）无效输入、依据未规定的输入与边界值

本组各条共用的前置条件同上一组；样本文件按测试数据需求在 ENV-4 的工作区里逐字节造好。

| 唯一标识符 | 英文名 | 目标 | 风险等级 | 前置条件 | 输入 | 预期结果 |
|:---|:---|:---|:---|:---|:---|:---|
| TC-11 | file_not_utf8 | 文件里的字节不是合法 UTF-8 时不给计数结果 | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py 非UTF8.md`（DATA-11 那个文件） | 标准输出上不出现计数行；标准错误上给出提示（措辞依据未规定，记录实际输出）；退出码非 0。若这份文件被当作空正文数出 `tokens: 0`，判为缺陷 |
| TC-12 | missing_path | 文件路径不存在时不给计数结果 | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py ./不存在的文件.md`（路径不存在） | 标准输出上不出现计数行；退出码非 0。若当作空正文数出 `tokens: 0`，判为缺陷 |
| TC-13 | path_is_directory | 路径指向一个目录时不给计数结果 | 高 | 见本组共用前置条件；ENV-4 工作区里先建好一个目录 | 执行 `python count_tokens.py ./样本目录` | 标准输出上不出现计数行；退出码非 0。若当作空正文数出 `tokens: 0`，判为缺陷 |
| TC-14 | stdin_not_utf8 | 标准输入里的字节不是合法 UTF-8 时不给计数结果 | 高 | 见本组共用前置条件；ENV-8 的管道可用 | 把 DATA-11 的字节接到标准输入后执行 `python count_tokens.py` | 标准输出上不出现计数行；标准错误上给出提示；退出码非 0 |
| TC-15 | same_bytes_two_channels | 同一段字符走字面文本与走文件两条通道时口径一致 | 高 | 见本组共用前置条件 | 先执行 `python count_tokens.py --text "$(printf '\xef\xbb\xbf')中文测试"`（按 DATA-17 取那段字面文本，开头的 U+FEFF 要真的生成出来），再执行 `python count_tokens.py 带标记.md`（按 DATA-10 取）——两处的字节序列完全相同 | 文件通道按去掉开头编码标记之后的正文算：`tokens: 2`、`chars: 4`（与 TC-9 同一口径）。字面文本通道给什么就数什么，`utf-8-sig` 只在读文件那一路上生效，开头的 U+FEFF 会算进正文：`tokens: 3`、`chars: 5`（与 DATA-17 记的一致）。两条通道对同一串字节给出不同的两个数，即判为缺陷——缺陷可能在测试项一侧，也可能在测试依据一侧。依据没有规定字面文本以 U+FEFF 开头时该怎么算，本用例按「编码标记不属于正文」这条读法固定要求：两条通道都应为 `tokens: 2`、`chars: 4`；执行的数不是这个，就把两条通道的实际值都记下来报给用户定口径 |
| TC-16 | three_channels_agree | 同一段正文经三条输入通道算出的两个数相同 | 高 | 见本组共用前置条件；ENV-8 的管道可用 | 用 DATA-1 那段正文依次跑三次：走 `--text` 给字面文本；把它写成一个不带编码标记的文件后走文件路径；把它接到标准输入 | 三次的标准输出都一样：`tokens: 3`、`chars: 5`；三次的退出码都是 0。任何一次与其余两次不同，判为缺陷 |
| TC-17 | empty_file | 空文件（正文长度 0 个字符，边界上的值） | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py 空文件.md`（DATA-12） | 标准输出两行：`tokens: 0`、`chars: 0`；标准错误为空；退出码 0 |
| TC-18 | single_char | 正文长度 1 个字符（边界外一个增量） | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py --text "中"`（按 DATA-16 给） | 标准输出两行：`tokens: 1`、`chars: 1`；退出码 0。这个字在 UTF-8 里占 3 字节，`chars` 给的是 1，不是 3 |
| TC-19 | truncated_bom | 文件开头 2 个字节，少一个字节的编码标记（边界下侧） | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py 截断标记.md`（DATA-14） | 标准输出上不出现计数行；标准错误上给出提示；退出码非 0。这 2 个字节不是合法 UTF-8，若被当作空正文数出 0，判为缺陷 |
| TC-20 | bom_only | 文件正好是 3 个字节的编码标记、后面没有正文（边界上的值） | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py 只有标记.md`（DATA-13） | 标准输出两行：`tokens: 0`、`chars: 0`；标准错误为空；退出码 0 |
| TC-21 | bom_plus_one_byte | 编码标记后接一个字节（边界上侧） | 高 | 见本组共用前置条件 | 执行 `python count_tokens.py 标记加一字.md`（DATA-15） | 标准输出两行：`tokens: 1`、`chars: 1`；退出码 0 |
| TC-27 | all_three_sources_given | 输入来源同时给出一路以上时取哪一路（依据未规定） | 高 | 见本组共用前置条件；ENV-8 的管道可用 | 把 DATA-1 那段正文同时按三路给出：执行 `python count_tokens.py 样本.md --text "你好，世界"`（文件按 DATA-9 取，字面文本按 DATA-1 取），并把 DATA-1 的字节接到标准输入 | 按本设计的读法「命令行给的先于标准输入、`--text` 先于文件路径」，取 `--text` 那一路：标准输出两行 `tokens: 3`、`chars: 5`，退出码 0。依据没有规定同时给出一路以上时取哪一路：给出的是另外两路之一的数，即判为缺陷；给出的确是 `--text` 那一路的数，也把「依据未规定」这一处记下来报给用户定口径 |
| TC-28 | file_and_stdin_available | 文件路径与标准输入同时可用时取哪一路（依据未规定） | 高 | 见本组共用前置条件；ENV-8 的管道可用 | 执行 `python count_tokens.py 样本.md`（按 DATA-9 取），同时把 DATA-1 的字节接到标准输入 | 按本设计的读法「文件路径先于标准输入」，取文件那一路：标准输出两行 `tokens: 19`、`chars: 61`，退出码 0。依据没有规定这一处，执行时把实际值记下来报给用户定口径；给出的若是管道里那段正文的数（`tokens: 3`、`chars: 5`），说明测试项取的是另一路 |
| TC-29 | empty_path_string | 文件路径给空字符串时走哪一路（依据未规定） | 高 | 见本组共用前置条件；ENV-8 的管道可用 | 执行 `python count_tokens.py ""`，并把 DATA-1 的字节接到标准输入 | 依据没有规定空字符串路径该怎么算。按当前测试项的实际行为，空字符串与「没给路径」是同一个判断结果，会转去读标准输入：标准输出两行 `tokens: 3`、`chars: 5`，退出码 0（标准输入接的是终端时会一直等下去，与那条按不可行项剔除的用法同一情形）。这一处按未规定功能被执行记下来报给用户：调用方本意是指一个文件、而那个变量为空时，测试项不报错，会静默数出管道里的内容 |

### （三）引擎就绪路径（TM-4 的判定规则）

本组各条都要一个新起的干净环境和一个可改动的技能目录副本，四条之间不能共用同一次安装的结果。

| 唯一标识符 | 英文名 | 目标 | 风险等级 | 前置条件 | 输入 | 预期结果 |
|:---|:---|:---|:---|:---|:---|:---|
| TC-22 | offline_bundled_wheel | 没有引擎、又不能联网时，用随包的 wheel 离线装上引擎后照常计数 | 中 | 按 ENV-5 新建一个没装引擎的干净环境；ENV-4 里放一份技能目录的副本，`wheels/` 下放着 ENV-3 那个 wheel；按 ENV-7 造出不能对外联网的运行条件 | 在干净环境里执行 `python <副本>/scripts/count_tokens.py --text "你好，世界"`（按 DATA-1 给） | 标准输出恰好两行：`tokens: 3`、`chars: 5`；退出码 0；整个执行过程没有访问网络。标准错误上可以出现一句安装提示（措辞依据未规定，记录实际输出）；标准输出上若除这两行之外还有别的内容，判为缺陷 |
| TC-23 | wheel_fails_then_pypi | 随包的 wheel 与本机平台不配时，转去联网装指定版本 | 中 | 按 ENV-5 新建干净环境；ENV-4 里把副本 `wheels/` 下那个 wheel 的文件名改成不适用于本机平台的标签（文件内容不动）；按 ENV-6 保证能连到软件源 | 在干净环境里执行 `python <副本>/scripts/count_tokens.py --text "你好，世界"` | 标准输出恰好两行：`tokens: 3`、`chars: 5`；退出码 0；标准错误上出现安装提示（措辞依据未规定，记录实际输出）。执行完之后那个环境里可以导入引擎。这条与真的在别的平台上跑有偏离：真实的平台不符由安装工具按本机标签判定，这条是改成别的标签让安装工具拒收 |
| TC-24 | no_wheel_pypi_direct | `wheels/` 下没有 wheel 时，直接联网装指定版本 | 中 | 按 ENV-5 新建干净环境；ENV-4 里把副本 `wheels/` 下那个 wheel 移走；按 ENV-6 保证能连到软件源 | 同 TC-23 的执行命令 | 同 TC-23 的预期结果 |
| TC-25 | both_install_paths_fail | 随包 wheel 不在、又不能联网时，以非 0 状态结束，不给计数结果 | 中 | 按 ENV-5 新建干净环境；ENV-4 里把副本的 `wheels/` 整个移走；按 ENV-7 造出不能对外联网的运行条件 | 同 TC-23 的执行命令 | 标准输出上不出现计数行；标准错误上留下失败信息（措辞依据未规定，记录实际输出）；退出码非 0。若装不上的情况下仍然打印 `tokens: 0` 这类结果，判为缺陷 |

### （四）端到端场景

| 唯一标识符 | 英文名 | 目标 | 风险等级 | 前置条件 | 输入 | 预期结果 |
|:---|:---|:---|:---|:---|:---|:---|
| TC-26 | main_scenario_markdown_file | 主场景：给一个文件路径，数出一份含中文与英文的 Markdown 文件的 token 数 | 中 | ENV-1 那台机器上引擎已经装好；ENV-4 的工作区里按 DATA-9 备好那份 Markdown 文件 | 执行 `python count_tokens.py 样本.md`（DATA-9） | 标准输出恰好两行：`tokens: 19`、`chars: 61`；标准错误为空；退出码 0 |

## 三、覆盖项 ↔ 用例

| 覆盖项编号 | 覆盖项描述 | 覆盖它的用例编号 |
|:---|:---|:---|
| TCOV-1 | 正文是纯 ASCII 文本、词与词之间有空格（E1） | TC-2 |
| TCOV-2 | 正文含中文，汉字与中文标点（E2） | TC-1 |
| TCOV-3 | 正文以汉字开头、后面接 ASCII（E3） | TC-3 |
| TCOV-4 | 正文里含官方词表的特殊 token（E4） | TC-4 |
| TCOV-5 | 空正文，0 个字符（E5） | TC-5 |
| TCOV-6 | 正文只有空白字符（E6） | TC-6 |
| TCOV-7 | 正文是代码文本（E7） | TC-7 |
| TCOV-8 | 正文含补充平面上的字符（E8） | TC-8 |
| TCOV-9 | 文件开头的编码标记 BOM 不计入正文（E9） | TC-9 |
| TCOV-10 | 正文首尾带空格（E10） | TC-10 |
| TCOV-11 | 文件里的字节不是合法 UTF-8（N1） | TC-11 |
| TCOV-12 | 文件路径不存在（N2） | TC-12 |
| TCOV-13 | 路径指向一个目录（N3） | TC-13 |
| TCOV-14 | 标准输入里的字节不是合法 UTF-8（N4） | TC-14 |
| TCOV-15 | 字面文本以 U+FEFF 开头，依据没规定这种情况该怎么算（N6） | TC-15 |
| TCOV-16 | `tokens` 的取值与随包词表的规范语义一致（O1） | TC-1 |
| TCOV-17 | `chars` 计的是字符个数，不是 UTF-8 字节数（O2） | TC-1、TC-18 |
| TCOV-18 | 有效输入产出 `tokens` 与 `chars` 两行（O3） | TC-1、TC-26 |
| TCOV-19 | 无效输入不产出计数行，以非 0 状态结束（O4） | TC-11、TC-12、TC-14 |
| TCOV-20 | 同一段正文经三条输入通道算出的两个数相同（O5） | TC-16 |
| TCOV-21 | 边界值：正文长度 0 个字符（边界上的值） | TC-17 |
| TCOV-22 | 边界值：正文长度 1 个字符（边界外一个增量） | TC-18 |
| TCOV-23 | 边界值：文件开头 2 个字节，少一个字节的编码标记（边界下侧） | TC-19 |
| TCOV-24 | 边界值：文件开头 3 个字节，正好一个完整的编码标记（边界上的值） | TC-20 |
| TCOV-25 | 边界值：文件开头 4 个字节，编码标记后接一个字节（边界上侧） | TC-21 |
| TCOV-26 | 判定规则 1：命令行给了 `--text` 时，按这段字面文本计数并输出两行 | TC-1 |
| TCOV-27 | 判定规则 2：给了文件路径、文件读得出来、字节能解码时，按文件计数并输出两行 | TC-16、TC-26 |
| TCOV-28 | 判定规则 3：文件读得出来但字节不是合法 UTF-8 时，不给计数结果，以非 0 状态结束 | TC-11 |
| TCOV-29 | 判定规则 4：文件读不出来（路径不存在，或路径是目录）时，不给计数结果，以非 0 状态结束 | TC-12、TC-13 |
| TCOV-30 | 判定规则 5：既没给 `--text` 也没给路径、标准输入接得上时，按标准输入计数；输入为空则给出 0 | TC-5、TC-16 |
| TCOV-31 | 判定规则 6：标准输入里的字节不是合法 UTF-8 时，不给计数结果，以非 0 状态结束 | TC-14 |
| TCOV-32 | 引擎判定规则 1：本机已经可以导入引擎时直接使用，不装、不联网，标准错误上不出安装提示 | TC-1 |
| TCOV-33 | 引擎判定规则 2：本机没装、随包 wheel 在且装得上时，离线安装这个 wheel | TC-22 |
| TCOV-34 | 引擎判定规则 3：本机没装、随包 wheel 装不上、联网装得上时，转去联网安装指定版本 | TC-23 |
| TCOV-35 | 引擎判定规则 4：本机没装、`wheels/` 下没有 wheel、联网装得上时，直接联网安装指定版本 | TC-24 |
| TCOV-36 | 引擎判定规则 5：两条安装路都不通时，以非 0 状态结束，不产出计数两行 | TC-25 |
| TCOV-37 | 场景：已经装好引擎的机器上数一个文件（主场景） | TC-26 |
| TCOV-38 | 场景：命令行直接给一段文本（备选场景） | TC-1 |
| TCOV-39 | 场景：把别的命令的输出接到管道（备选场景） | TC-16 |
| TCOV-40 | 场景：没有引擎、不能联网的机器上首次使用（备选场景） | TC-22 |
| TCOV-41 | 场景：没有引擎、随包 wheel 不适用的机器上首次使用（备选场景） | TC-23 |
| TCOV-42 | 场景：给的文件不是合法 UTF-8（备选场景） | TC-11 |
| TCOV-43 | 输入来源同时给出一路以上：`--text` 与文件路径同时给出，或 `--text` 与标准输入同时可用，依据没规定按哪一路算 | TC-27 |
| TCOV-44 | 输入来源同时给出一路以上：文件路径与标准输入同时可用，依据没规定按哪一路算 | TC-28 |
| TCOV-45 | 文件路径给空字符串，依据没规定这种情况该怎么算 | TC-29 |
