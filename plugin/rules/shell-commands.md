## 跑命令：路径写全，不用 cd

**每条命令里的路径写全**（`C:/work/x/y.py` 这样的绝对路径）——这条命令放在哪个目录下跑都是同一句。

**不用 `cd` 进目录再做事**，也不必靠它去凑相对路径。工作目录一动，后面每条命令的起点就跟着变；
工具会把它重置回原处并提示，这一趟往返是白花的。

要指定目录的地方用工具自带的参数：`git -C <仓库>`、`make -C <目录>`、`npm --prefix <目录>`。
